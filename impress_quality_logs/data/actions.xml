<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Fetch data action required for every submodel of the mixin 
    <record id="fetch_data" model="ir.actions.server">
        <field name="name">Fetch data</field>
        <field name="model_id" model="ir.model" eval="obj().env.ref('impress_quality_logs.model_hpp_log_line').id"/>
        <field name="binding_type">action</field>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            for record in records:
                record.action_fetch_related_values()
        </field>
    </record>
-->
</odoo>
