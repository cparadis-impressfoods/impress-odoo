<?xml version='1.0' encoding="utf-8" ?>
<odoo>
        <!-- Change to the task list view.
        <record id="impress_project_billing_portal_tasks" model="ir.ui.view">
            <field name="name">impress_project_billing_portal_tasks</field>
            <field name="model" />
            <field name="inherit_id" ref="project.portal_tasks_list" />
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <xpath
                expr="//thead/tr/t[@t-if='not project or project.allow_timesheets']"
                position="replace"
            >
                    <t t-if="not project or project.allow_timesheets">
                        <th t-if="is_uom_day" class="text-end">Produced / Planned</th>
                        <th t-else="" class="text-end">Produced / Planned</th>
                    </t>
                </xpath>

                <xpath
                expr="//tbody/t/tr/td[@t-if='not project or project.allow_timesheets']/t/t[2]"
                position="replace"
            >
                    <t t-else="">
                        <span t-field="task.effective_hours" />
                        <t
                        t-if="task.allocated_hours > 0"
                    > /                            <span
                            t-field="task.allocated_hours"
                        />
                        </t>
                    </t>
                </xpath>

                <xpath expr="//tbody/t/tr/td[4]" position="replace" />

            </field>
        </record>
        -->
</odoo>
