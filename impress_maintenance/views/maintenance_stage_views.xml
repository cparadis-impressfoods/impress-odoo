<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!-- View maintenance.stage tree -->
    <record id="view_maintenance_stage_tree" model="ir.ui.view">
        <field name="name">view.maintenance.stage.tree</field>
        <field name="model">maintenance.stage</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_stage_view_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='done']" position="after">
                <field name="color" widget="color_picker" />
            </xpath>
        </field>
    </record>

</odoo>
